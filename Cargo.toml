[package]
name = "portfolio-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
pulldown-cmark = "0.12"
syntect = "5"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["fs"] }
